package com.fabricmanagement.inventory.common.domain;

/** LotEndRule ve benzeri kurallarda kullanılan modül tipi enum'u (CR-10-07). */
public enum ProductionModuleType {
  FIBER,
  YARN,
  FABRIC,
  DYE_FINISHING
}
