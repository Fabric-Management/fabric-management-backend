package com.fabricmanagement.production.core.batch.domain;

/** Document meaning; independent from the BATCH/FACILITY/SUPPLIER coverage axis. */
public enum BatchCertificateKind {
  SCOPE,
  TRANSACTION
}
